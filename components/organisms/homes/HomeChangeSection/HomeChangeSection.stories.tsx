// eslint-disable-next-line import/named
import { ComponentMeta, ComponentStoryObj } from '@storybook/react'
import HomeChangeSection from './HomeChangeSection'

export default {
  title: 'components / organisms / homes / HomeChangeSection',
  component: HomeChangeSection
} as ComponentMeta<typeof HomeChangeSection>

export const Index: ComponentStoryObj<typeof HomeChangeSection> = {
  args: {}
}
