// eslint-disable-next-line import/named
import { ComponentMeta, ComponentStoryObj } from '@storybook/react'
import SocietySummarySection from './SocietySummarySection'

export default {
  title: 'components / molecules / society / SocietyContentCatchPhrase',
  component: SocietySummarySection
} as ComponentMeta<typeof SocietySummarySection>

export const Index: ComponentStoryObj<typeof SocietySummarySection> = {
  args: {}
}
