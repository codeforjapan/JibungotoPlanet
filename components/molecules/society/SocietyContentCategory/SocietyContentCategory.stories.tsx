// eslint-disable-next-line import/named
import { ComponentMeta, ComponentStoryObj } from '@storybook/react'
import SocietyContentCategory from './SocietyContentCategory'

export default {
  title: 'components / molecules / society / SocietyContentCatchPhrase',
  component: SocietyContentCategory
} as ComponentMeta<typeof SocietyContentCategory>

export const Index: ComponentStoryObj<typeof SocietyContentCategory> = {
  args: {}
}
